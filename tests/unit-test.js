const chai = require('chai');
const expect = chai.expect;
const assert = require('chai').assert
const {sortUp, sortDown } = require('../public/index.js');

describe('App', () => {


  it('should have a sortUp method', () => {

})
